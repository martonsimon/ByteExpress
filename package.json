{
  "name": "byte-express",
  "version": "0.1.7",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "author": "martonsimon",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/martonsimon/ByteExpress.git#ts_dev"
  },
  "description": "A description",
  "files": [
    "build/**/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^14.11.2",
    "concurrently": "^8.1.0",
    "gts": "^3.1.1",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typedoc": "^0.24.8",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "start": "nodemon build/app.js",
    "build": "tsc -p .",
    "dev": "concurrently \"npm run build -- --watch\" \"npm run start\"",
    "jest-test": "jest",
    "jest-test-dev": "npm run build && npm run jest-test -- --watch",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "lint_": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run build",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  },
  "dependencies": {
    "tslog": "^4.8.2"
  }
}
